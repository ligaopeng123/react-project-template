/**
 *  用户登录模块
 */
import React from 'react';
import JSEncryptLogin from "@layouts/UserLayout/JSEncryptLogin";

const UserLayout: React.FC<any> = (props: any) => {
    return (
        <JSEncryptLogin/>
    );
};

export default UserLayout;
